Added function to multiply numbers, format multiply(a,b).
With the multiply function, a square function is made. It takes one input number and returns the square value of this number.  

The subtraction function takes an input of two variables, 'a' and 'b'. The function will then perform a-b. \
Added function distance_from_zero(x) by subtracting a given number by zero.

Function A adds two seperate digits a and b. The extension of this function takes a list of numbers and sums that list.
